📌 To-Do List Application (Python + SQLite)
A simple command-line To-Do List app built using Python and SQLite. It allows users to manage daily tasks with options to add, view, update, delete, backup, and restore tasks.

Key Features:
Add Tasks: Save new tasks with a default “pending” status.
View Tasks: Display all tasks with their ID, name, and status.
Mark as Done: Update task status to “done” using task ID.
Delete Tasks: Remove tasks permanently from the database.
Backup to JSON: Export all tasks to a JSON file.
Restore from JSON: Restore tasks from backup without duplicates.

Flow:
Users interact through a simple menu that performs all task operations using SQLite database handling.
